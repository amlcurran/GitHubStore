apply plugin: 'java'

sourceSets {
    test {
        java {
            srcDirs = ['src/main/test']
        }
    }
}

dependencies {
    testCompile 'junit:junit:4.11'
    compile 'com.google.code.gson:gson:2.3'
}